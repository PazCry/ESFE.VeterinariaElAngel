<Window x:Class="InterfazDeUsuarioUI.VentanaEspecie"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InterfazDeUsuarioUI"
        mc:Ignorable="d"
        Title="VentanaEspecie" Height="527" Width="800">
    <Window.Resources>
        <!-- Gradientes y estilos -->
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFC699F3" Offset="0"/>
            <GradientStop Color="#FFC699F3" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#BA68C8" Offset="0"/>
            <GradientStop Color="#9C27B0" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="SaveButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#66BB6A" Offset="0"/>
            <GradientStop Color="#4CAF50" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="EditButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFA726" Offset="0"/>
            <GradientStop Color="#FF9800" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DeleteButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#EF5350" Offset="0"/>
            <GradientStop Color="#F44336" Offset="1"/>
        </LinearGradientBrush>

        <!-- Estilo para botones principales -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="2" BlurRadius="4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para TextBox moderno -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1BEE7"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center"
                                        Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#BA68C8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para ComboBox -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1BEE7"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" 
                                            Background="Transparent"
                                            BorderThickness="0"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press">
                                    <Path x:Name="Arrow" 
                                          Fill="#9C27B0" 
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          HorizontalAlignment="Right" 
                                          Margin="0,0,10,0"/>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"
                                                Margin="10,0"/>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True">
                                    <Border Background="White" 
                                            BorderBrush="#E1BEE7" 
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            MaxHeight="200">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#BA68C8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para DataGrid -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1BEE7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FCE4EC"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>

        <!-- Estilo para encabezados de DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource HeaderGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Estilo para filas de DataGrid -->
        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="35"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3E5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E1BEE7"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" 
                Background="#F8BBD9" 
                CornerRadius="12" 
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="3" BlurRadius="6"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" Margin="20,15">
                <Path Fill="White" 
                      Width="24" Height="24" 
                      Margin="0,0,15,0"
                      Data=""/>
                <TextBlock Text="GESTIÓN DE ESPECIES" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Formulario de datos -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="12" 
                Padding="25" Height="170" VerticalAlignment="Center" HorizontalAlignment="Center" Width="760">
            <Border.Effect>
                <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="8"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>


                <!-- Campos del formulario -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Primera fila de campos -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBox x:Name="txtIdEspecie" Style="{StaticResource ModernTextBox}" Visibility="Collapsed"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5,0"/>
                </Grid>

                <!-- Botones de acción -->
                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,25,0,0"/>
                <Button x:Name="btnGuardar" 
                            Content="💾 Guardar" 
                            Background="#FFA8E6CF"
                            Style="{StaticResource ModernButton}" Margin="20,51,0,0" Grid.Row="2" Height="40" VerticalAlignment="Top" HorizontalAlignment="Left" Width="164" Click="btnGuardar_Click_1"/>
                <Button x:Name="btnEliminar" 
                            Content="🗑️ Eliminar" 
                            Background="#FFFFB3D1"
                            Style="{StaticResource ModernButton}" Margin="531,45,26,-10" Grid.Row="2" Click="btnEliminar_Click_1"/>
                <Button x:Name="btnModificar" 
                            Content="✏️ Modificar" 
                            Background="#FFFFD3A5"
                            Style="{StaticResource ModernButton}" Margin="270,45,284,-10" Grid.Row="2" Cursor="Hand" Click="btnModificar_Click_1"/>
                <TextBlock Text="Tipo de Especie" FontWeight="SemiBold" Foreground="#666" Margin="11,-7,472,13"/>
                <TextBox x:Name="txtNombre" Style="{StaticResource ModernTextBox}" Margin="11,14,0,0" Grid.RowSpan="3" HorizontalAlignment="Left" Width="673" Height="46" VerticalAlignment="Top"/>
            </Grid>
        </Border>

        <!-- DataGrid para mostrar especies -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="12" Margin="0,26,0,26">
            <Border.Effect>
                <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="8"/>
            </Border.Effect>

            <Grid Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="154*"/>
                    <ColumnDefinition Width="24*"/>
                    <ColumnDefinition Width="177*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Título de la tabla -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15" Grid.ColumnSpan="3">
                    <Path Fill="#9C27B0" 
                          Width="20" Height="20" 
                          Margin="0,0,10,0"
                          Data=""/>

                </StackPanel>

                <!-- DataGrid -->
                <DataGrid x:Name="dgEspecies"
                          Style="{StaticResource ModernDataGrid}"
                          AutoGenerateColumns="False" Margin="0,-19,0,0" Grid.ColumnSpan="3" Height="145" VerticalAlignment="Top" Grid.RowSpan="2" SelectionChanged="dgEspecies_SelectionChanged_1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                            Binding="{Binding Id}" 
                                            Width="80"
                                            IsReadOnly="True"/>
                        <DataGridTextColumn Header="SampleStringA" 
                                            Binding="{Binding SampleStringA}" 
                                            Width="*"
                                            IsReadOnly="True"/>
                        <DataGridTextColumn Header="SampleStringB" 
                                            Binding="{Binding SampleStringB}" 
                                            Width="*"
                                            IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Header="Activo" 
                                                Binding="{Binding IsActive}" 
                                                Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
