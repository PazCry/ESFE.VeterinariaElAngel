<Window x:Class="InterfazDeUsuarioUI.VentanaExpediente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InterfazDeUsuarioUI"
        mc:Ignorable="d"
        Title="VentanaExpediente" Height="1278" Width="1026">
    <Window.Resources>
        <!-- Estilos para botones con gradientes -->
        <Style x:Key="GradientButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para TextBox con fondo rosa -->
        <Style x:Key="PinkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="#FFB5C5" CornerRadius="15" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center" 
                                        Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para ComboBox -->
        <Style x:Key="PinkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="#FFB5C5" CornerRadius="15" BorderThickness="0">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" 
                                            Grid.Column="2" 
                                            Focusable="false" 
                                            ClickMode="Press" 
                                            IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                            Background="Transparent" 
                                            BorderThickness="0"/>
                                <ContentPresenter x:Name="ContentSite" 
                                                IsHitTestVisible="False" 
                                                Content="{TemplateBinding SelectionBoxItem}" 
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                                Margin="10,3,23,3" 
                                                VerticalAlignment="Center" 
                                                HorizontalAlignment="Left"/>
                                <TextBox x:Name="PART_EditableTextBox" 
                                        Style="{x:Null}" 
                                        HorizontalAlignment="Left" 
                                        VerticalAlignment="Center" 
                                        Margin="3,3,23,3" 
                                        Focusable="True" 
                                        Background="Transparent" 
                                        Visibility="Hidden" 
                                        IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup x:Name="Popup" 
                                      Placement="Bottom" 
                                      IsOpen="{TemplateBinding IsDropDownOpen}" 
                                      AllowsTransparency="True" 
                                      Focusable="False" 
                                      PopupAnimation="Slide">
                                    <Grid x:Name="DropDown" 
                                          SnapsToDevicePixels="True" 
                                          MinWidth="{TemplateBinding ActualWidth}" 
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" 
                                               Background="White" 
                                               BorderThickness="1" 
                                               BorderBrush="#CCCCCC" 
                                               CornerRadius="5"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Gradientes -->
        <LinearGradientBrush x:Key="PinkHeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF9BB5" Offset="0"/>
            <GradientStop Color="#FFC1D4" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="GreenGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#4CAF50" Offset="0"/>
            <GradientStop Color="#66BB6A" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BlueGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#2196F3" Offset="0"/>
            <GradientStop Color="#42A5F5" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="OrangeGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF9800" Offset="0"/>
            <GradientStop Color="#FFB74D" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="RedGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F44336" Offset="0"/>
            <GradientStop Color="#EF5350" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PurpleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#7B1FA2" Offset="0"/>
            <GradientStop Color="#9C27B0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Border Background="White" CornerRadius="8" Margin="10,10,0,10" HorizontalAlignment="Left" Width="1024">

        <Grid Margin="2,0,-2,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#F8BBD9" CornerRadius="8,8,0,0" Padding="20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="📁" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="GESTIÓN DE EXPEDIENTES" 
                              Foreground="White" 
                              FontSize="16" 
                              FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Form Section -->
            <Grid Grid.Row="1" Margin="25,25,0,0" Height="395" VerticalAlignment="Top" HorizontalAlignment="Left" Width="773">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form Title -->
                <TextBlock Grid.Row="0" Text="FORMULARIO DE DATOS" 
                          Foreground="#666" 
                          FontSize="11" 
                          FontWeight="Bold" 
                          Margin="0,-16,0,31"/>

                <!-- Form Fields -->
                <Grid Grid.Row="1" Height="290" VerticalAlignment="Top" HorizontalAlignment="Center" Width="773" Grid.RowSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Primera fila -->
                    <StackPanel Grid.Row="0" Margin="5,5,0,0" HorizontalAlignment="Left" Width="377" Height="53" VerticalAlignment="Top">
                        <TextBox x:Name="txtId" Style="{StaticResource PinkTextBoxStyle}" Height="49" Foreground="Black" Background="#FFFFD4E9" Width="368"/>
                    </StackPanel>

                    <StackPanel Margin="392,2,0,2" HorizontalAlignment="Left" Width="376">
                        <TextBlock Foreground="#666" FontSize="11" Width="209"><Run Language="es-sv" Text="Mascota"/><Run Text=":"/></TextBlock>

                    </StackPanel>

                    <!-- Segunda fila -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5,5,391,5"/>

                    <StackPanel Grid.Row="1" Margin="392,5,5,5"/>

                    <!-- Tercera fila -->
                    <StackPanel Grid.Row="2" Margin="5,5,0,0" HorizontalAlignment="Left" Width="377" Height="54" VerticalAlignment="Top"/>

                    <!-- Cuarta fila - Descripción (span completo) -->
                    <StackPanel Grid.Row="3" Margin="0,5,0,0" Height="88" VerticalAlignment="Top" HorizontalAlignment="Center" Width="763"/>

                    <ComboBox x:Name="cbxEstado" Margin="470,10,0,0" Grid.Row="2" BorderBrush="#FFF4A6CD" HorizontalAlignment="Left" Width="372" Height="54" VerticalAlignment="Top" Grid.RowSpan="2">
                        <ComboBox.Background>
                            <LinearGradientBrush EndPoint="0,1">
                                <GradientStop Color="#FFF0F0F0"/>
                                <GradientStop Color="#FFF4A6CD" Offset="1"/>
                            </LinearGradientBrush>
                        </ComboBox.Background>
                    </ComboBox>
                    <ComboBox x:Name="cbxIdCliente" HorizontalAlignment="Left" Margin="9,17,0,0" Grid.Row="1" Width="372" Height="38" VerticalAlignment="Top" Grid.RowSpan="2">
                        <ComboBox.Background>
                            <LinearGradientBrush EndPoint="0,1">
                                <GradientStop Color="#FFF0F0F0"/>
                                <GradientStop Color="#FFF4A6CD" Offset="1"/>
                            </LinearGradientBrush>
                        </ComboBox.Background>
                    </ComboBox>
                    <TextBlock Text="Estado:" Foreground="#666" FontSize="11" RenderTransformOrigin="0.504,0.616" Margin="470,40,163,56" Grid.RowSpan="2" Grid.Row="1"/>
                    <DatePicker x:Name="dtpFechaAtencion" Margin="10,40,387,87" Padding="10,8" BorderThickness="0" FontSize="12" Grid.RowSpan="2" Grid.Row="2">
                        <DatePicker.Template>
                            <ControlTemplate TargetType="{x:Type DatePicker}">
                                <Border Background="#FFB5C5" CornerRadius="15" BorderThickness="0">
                                    <Grid>
                                        <Button x:Name="PART_Button" 
                                                   HorizontalAlignment="Stretch" 
                                                   VerticalAlignment="Stretch" 
                                                   Background="Transparent" 
                                                   BorderThickness="0"/>
                                        <DatePickerTextBox x:Name="PART_TextBox" 
                                                             Foreground="White" 
                                                             Background="Transparent" 
                                                             BorderThickness="0" 
                                                             Margin="10,0"/>
                                        <Popup x:Name="PART_Popup"/>
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </DatePicker.Template>
                    </DatePicker>
                    <TextBlock Text="Fecha Inicio:" Foreground="#666" FontSize="11" Margin="15,24,381,77" Grid.Row="2" Grid.RowSpan="2"/>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="304,196,0,0" Height="137" VerticalAlignment="Top"/>
                <ComboBox x:Name="cbxIdMascota" Margin="474,25,0,0" Grid.Row="2" Background="#FFFFB5C5" Height="54" VerticalAlignment="Top" HorizontalAlignment="Left" Width="370"/>
                <Button x:Name="btnGuardar" Content="✓ Guardar" 
                           Background="#FFA8E6CF" 
                           Style="{StaticResource GradientButtonStyle}" Margin="37,299,0,0" Grid.Row="2" HorizontalAlignment="Left" Width="134" Height="49" VerticalAlignment="Top" Click="btnGuardar_Click_1"/>
                <Button x:Name="btnBuscar" Content="🔍 Buscar" 
                           Background="#FFA8C8EC" 
                           Style="{StaticResource GradientButtonStyle}" Margin="232,299,0,0" Grid.Row="2" HorizontalAlignment="Left" Width="144" Height="51" VerticalAlignment="Top" Click="btnBuscar_Click_1"/>
                <Button x:Name="btnModificar" Content="✏ Modificar" 
                           Background="#FFFFD3A5" 
                           Style="{StaticResource GradientButtonStyle}" Margin="414,299,0,0" Grid.Row="2" HorizontalAlignment="Left" Width="144" Height="50" VerticalAlignment="Top" Click="btnModificar_Click_1"/>
                <Button x:Name="btnEliminar" Content="🗑 Eliminar" 
                           Background="#FFFFB3D1" 
                           Style="{StaticResource GradientButtonStyle}" Margin="628,301,0,0" Grid.Row="2" HorizontalAlignment="Left" Width="134" Height="48" VerticalAlignment="Top" Click="btnEliminar_Click_1"/>
                <TextBlock Text="Num Expediente:" Foreground="#666" FontSize="11" Margin="1,9,395,6"/>
            </Grid>

            <!-- Search Bar -->
            <Border x:Name="txtBuscar" Grid.Row="1" Background="#E8EAF6" CornerRadius="8" Margin="25,410,15,0" Padding="15" Height="66" Grid.RowSpan="2" VerticalAlignment="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="588*"/>
                        <ColumnDefinition Width="463*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" 
                            x:Name="txtBuscar1"
                            Background="White" 
                            BorderThickness="0" 
                            Padding="15,10" 
                            FontSize="12"
                            VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="0,0,74,0">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBox">
                                <Border Background="{TemplateBinding Background}" 
                                       CornerRadius="20" 
                                       BorderThickness="0">
                                    <ScrollViewer x:Name="PART_ContentHost" 
                                                VerticalAlignment="Center" 
                                                Margin="15,0"/>
                                </Border>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>

                    <Button x:Name="btnBuscar1" 
                           Content="🔍" 
                           Background="#FFA97BCA" 
                           Foreground="White"
                           BorderThickness="0" 
                           Padding="15,10" 
                           Margin="356,0,0,0"
                           Cursor="Hand" HorizontalAlignment="Left" Width="54" Height="36" VerticalAlignment="Center" Grid.Column="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                       CornerRadius="20" 
                                       BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Records Section Header -->
            <Border x:Name="btnRegistroExpediente" Grid.Row="3" Background="#FFA97BCA" Margin="20,9,20,1" CornerRadius="5" Padding="15">
                <TextBlock Text="REGISTROS DE EXPEDIENTES" 
                          Foreground="White" 
                          FontWeight="Bold" 
                          HorizontalAlignment="Center" 
                          FontSize="14"/>
            </Border>

            <!-- Data Grid -->
            <Border Grid.Row="4" Background="White" CornerRadius="5" Margin="20,0,20,20">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>

                <DataGrid x:Name="dgvListarExpediente" 
                         AutoGenerateColumns="False" 
                         HeadersVisibility="Column"
                         GridLinesVisibility="Horizontal"
                         RowHeaderWidth="0"
                         BorderThickness="0"
                         FontSize="12"
                         RowHeight="35" Height="345" VerticalAlignment="Top" Background="#FFF0F0F0" Margin="-10,10,10,0">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="Foreground" Value="#666"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#DDD"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8F9FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Num Expediente" Binding="{Binding NumExpediente}" Width="120"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="150"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                        <DataGridTextColumn Header="Fecha Inicio" Binding="{Binding FechaInicio}" Width="100"/>
                        <DataGridTextColumn Header="Prioridad" Binding="{Binding Prioridad}" Width="100"/>
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <TextBox x:Name="txtDescripcionConsulta" 
                                TextWrapping="Wrap" 
                                AcceptsReturn="True" 
                                VerticalScrollBarVisibility="Auto"
                                Padding="10,8"
                                Margin="35,265,35,123"
                                BorderThickness="0"
                                Foreground="Black"
                                FontSize="12" Grid.Row="1">
                <TextBox.Template>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="#FFFFD4E9" CornerRadius="10" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                                    Margin="10"/>
                        </Border>
                    </ControlTemplate>
                </TextBox.Template>
            </TextBox>
            <TextBlock Text="Descripción:" Foreground="#666" FontSize="11" Margin="35,241,322,202" Grid.Row="1"/>
            <TextBlock Text="Cliente:" Foreground="#666" FontSize="11" Margin="39,116,704,336" Grid.Row="1"/>
        </Grid>
    </Border>

</Window>
